name: Publish HWM 0.2.5.15 DEV Hotfix

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/publish-hwm-0.2.5.15-dev.yml'

permissions:
  contents: write

env:
  RELEASE_VERSION: '0.2.5.15'
  SERVICE_VERSION: '0.2.5.11'
  TRAY_VERSION: '1.0.0.11'

jobs:
  publish-dev:
    runs-on: windows-latest

    steps:
      - name: Checkout public update feed
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout private HWM source
        uses: actions/checkout@v4
        with:
          repository: Quantumrooster/HWM
          ref: main
          token: ${{ secrets.HWM_PRIVATE_READ_TOKEN }}
          path: _hwm

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Build DEV hotfix package
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          dotnet publish _hwm/HeadlineWorkstationMonitor/HeadlineWorkstationMonitor.csproj -c Release -r win-x64 --self-contained true -o _out/tray

          $trayExe = Join-Path $PWD '_out/tray/HeadlineWorkstationMonitor.exe'
          if (-not (Test-Path $trayExe)) { throw 'Published HWM tray executable not found.' }

          $trayVersion = (Get-Item $trayExe).VersionInfo.FileVersion
          if ($trayVersion -notlike "$env:TRAY_VERSION*") { throw "Unexpected HWM tray version: $trayVersion" }

          $stablePackage = Join-Path $PWD 'feed/releases/Headline-HWM-Service-v0.2.5.14.zip'
          if (-not (Test-Path $stablePackage)) { throw 'Current stable HWM package not found.' }

          $stableExtract = Join-Path $PWD '_out/stable-current'
          Expand-Archive -Path $stablePackage -DestinationPath $stableExtract -Force

          $serviceExe = Join-Path $stableExtract 'HeadlineWorkstationService.exe'
          if (-not (Test-Path $serviceExe)) { throw 'Stable HWM service executable not found.' }

          $serviceVersion = (Get-Item $serviceExe).VersionInfo.FileVersion
          if ($serviceVersion -notlike "$env:SERVICE_VERSION*") { throw "Unexpected stable HWM service version: $serviceVersion" }

          $devDir = Join-Path $PWD 'feed/dev'
          New-Item -ItemType Directory -Path $devDir -Force | Out-Null

          $zipName = "Headline-HWM-Service-v$env:RELEASE_VERSION-DEV.zip"
          $zipPath = Join-Path $devDir $zipName
          Compress-Archive -Path @($serviceExe, $trayExe) -DestinationPath $zipPath -CompressionLevel Optimal -Force

          $hash = (Get-FileHash $zipPath -Algorithm SHA256).Hash.ToUpperInvariant()
          $size = (Get-Item $zipPath).Length
          if ($size -ge 104857600) { throw "DEV release package is too large for GitHub: $size bytes" }

          $manifest = [ordered]@{
            schemaVersion = 1
            channel = 'dev'
            releaseVersion = $env:RELEASE_VERSION
            publishedUtc = (Get-Date).ToUniversalTime().ToString('o')
            urgent = $false
            allowDowngrade = $false
            minimumUpdaterVersion = '0.1.2.0'
            releaseNotes = 'HWM DEV 0.2.5.15 pairs the current stable service 0.2.5.11 with tray 1.0.0.11. The tray fixes the long-idle first-open crash by reusing one WinForms ToolTip instead of creating new ToolTip objects during repeated background cloud-status refreshes. Existing first-show and tray reliability protections remain unchanged.'
            components = [ordered]@{
              hwmServiceVersion = $env:SERVICE_VERSION
              hwmTrayVersion = $env:TRAY_VERSION
              hcsAutomationVersion = ''
            }
            package = [ordered]@{
              url = "https://quantumrooster.github.io/HWM-Updates/dev/$zipName"
              sha256 = $hash
              sizeBytes = $size
            }
          }

          $manifest | ConvertTo-Json -Depth 8 | Set-Content (Join-Path $devDir 'dev.json') -Encoding UTF8

          Write-Host "Built DEV HWM $env:RELEASE_VERSION"
          Write-Host "Service: $serviceVersion"
          Write-Host "Tray: $trayVersion"
          Write-Host "SHA256: $hash"
          Write-Host "Size: $size"

      - name: Commit DEV hotfix
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          git config user.name 'Headline HWM Release Bot'
          git config user.email '248761542+Quantumrooster@users.noreply.github.com'
          git add "feed/dev/Headline-HWM-Service-v$env:RELEASE_VERSION-DEV.zip" feed/dev/dev.json

          if (git diff --cached --quiet) {
            Write-Host 'DEV hotfix is already published.'
            exit 0
          }

          git commit -m "release: publish HWM $env:RELEASE_VERSION to DEV"
          git pull --rebase origin main
          git push origin HEAD:main
